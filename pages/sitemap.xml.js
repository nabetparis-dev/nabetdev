import { readJson } from "../lib/serverData";const SITE_URL="https://www.nabet-paris.com";function one(loc,p="0.8",c="weekly"){return `<url><loc>${SITE_URL}${loc}</loc><changefreq>${c}</changefreq><priority>${p}</priority></url>`;}export default function Sitemap(){}export async function getServerSideProps({res}){const products=readJson("products");const categories=readJson("categories");const urls=[one("/","1.0","daily"),one("/contact","0.7","monthly"),one("/terms","0.4","monthly"),...categories.map(c=>one(`/category/${c.id}`,"0.9","weekly")),...products.map(p=>one(`/product/${p.id}`,"0.8","weekly"))];res.setHeader("Content-Type","text/xml");res.write(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.join("")}</urlset>`);res.end();return{props:{}};}